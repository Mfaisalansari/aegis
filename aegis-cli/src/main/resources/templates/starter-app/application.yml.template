# Generated by `aegis init` — see AEGIS's USAGE.md §0/§4 for the full field reference.

application:
  baseUrl: {{BASE_URL}}
  # username: your_username        # or env:YOUR_ENV_VAR — see USAGE.md §8c "Secret management"
  # password: your_password
  # successUrlContains: dashboard  # a substring of the URL AEGIS should treat as "mission succeeded"

browser:
  type: chromium
  headless: false

mission:
  strategy: adaptive

report:
  directory: reports
